alias nvrun="__NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia $1"
alias venv="source .venv/bin/activate"
alias matrix="nvim -c Matrix"
alias update="sudo zypper ref; sudo zypper dup; sudo flatpak update; sudo fwudmgr update;"

#PS1="\t \W$ "
PS1="../\W$ "

